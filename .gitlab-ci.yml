image: haskell

variables:
  STACK_ROOT: "${CI_PROJECT_DIR}/.stack"

cache:
  paths:
    - .stack-work
    - .stack

before_script:
  - apt-get update
  - apt-get install libsdl2-dev libsdl2-ttf-dev
  - stack setup

test:
  script:
    - stack test

build:
  script:
    - stack build
